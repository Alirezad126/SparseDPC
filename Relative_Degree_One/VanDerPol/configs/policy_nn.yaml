# NN-DPC uses the exact SD-DPC data, horizon, objective, constraints, and coefficients.
inherit: policy.yaml
policy_type: nn
layers: [32, 32]
activation: gelu
lr: 0.005
epochs: 1000
warmup: 100
patience: 200
